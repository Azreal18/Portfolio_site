{
  "name": "portfolio-site",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build && echo Build complete. Deploy the dist folder to GitHub Pages."
  },
  "devDependencies": {
    "vite": "^5.0.0"
  }
}